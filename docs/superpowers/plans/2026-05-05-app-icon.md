# App icon and favicon — implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the placeholder Vite favicon and ship a tiled Ace-of-Diamonds icon across web, iOS, and Android, generated from a single SVG source via `@capacitor/assets`.

**Architecture:** Hand-authored SVG sources live in `resources/`. A small Node script (`scripts/build-icons.mjs`) rasterizes them to PNGs at the sizes Capacitor expects, copies the master to `public/favicon.svg` for the web build, and invokes `capacitor-assets generate` to populate `ios/`/`android/`. Source SVGs use the generic `serif` family so rasterization works with any installed system font (Linux fontconfig falls back to DejaVu Serif which has Unicode suit glyphs).

**Tech Stack:** SVG (hand-authored), Node 22 + `@resvg/resvg-js` (PNG rasterizer, no native compile), `@capacitor/assets` (Capacitor's official icon/splash generator), Vite for the web build.

**Spec:** `docs/superpowers/specs/2026-05-05-app-icon-design.md`

---

## File structure

| Path                                  | New / Modified | Purpose                                    |
| ------------------------------------- | -------------- | ------------------------------------------ |
| `resources/icon.svg`                  | new            | Master full-bleed icon (1024² SVG)        |
| `resources/icon-foreground.svg`       | new            | Card only, transparent bg, 78% scale      |
| `resources/icon-background.svg`       | new            | Felt vignette only, full bleed            |
| `resources/splash.svg`                | new            | Master icon centered on felt at 33%       |
| `scripts/build-icons.mjs`             | new            | SVG → PNG + capacitor-assets pipeline     |
| `public/favicon.svg`                  | new            | Master copy, served at `/favicon.svg`     |
| `public/favicon-32.png`               | new            | 32 × 32 PNG fallback                      |
| `public/favicon-16.png`               | new            | 16 × 16 PNG fallback                      |
| `index.html`                          | modified       | Add `<link rel="icon">` tags              |
| `package.json`                        | modified       | Add devDeps + `assets:generate` script    |
| `.gitignore`                          | modified       | Ignore `resources/*.png` (intermediates)  |
| `docs/superpowers/specs/native-build-notes.md` | modified | Replace generic icon notes with new pipeline |

---

## Task 1: Master icon SVG

**Files:**
- Create: `resources/icon.svg`

- [ ] **Step 1: Create the master SVG**

Write `resources/icon.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="1024" height="1024">
  <defs>
    <radialGradient id="feltGrad" cx="50%" cy="38%" r="75%">
      <stop offset="0%" stop-color="#1c4a2a"/>
      <stop offset="60%" stop-color="#0f3818"/>
      <stop offset="100%" stop-color="#072010"/>
    </radialGradient>
    <filter id="cardShadow" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="2" dy="5" stdDeviation="3" flood-opacity="0.45"/>
    </filter>
  </defs>
  <rect width="200" height="200" rx="40" fill="url(#feltGrad)"/>
  <g transform="rotate(-7 100 100)">
    <g filter="url(#cardShadow)">
      <rect x="55" y="32" width="90" height="135" rx="9" fill="#fffaf0"/>
    </g>
    <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
    <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
    <g transform="rotate(180 100 99.5)">
      <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
      <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
    </g>
    <text x="100" y="135" font-size="64" text-anchor="middle" fill="#c41e3a" font-family="serif">♦</text>
  </g>
</svg>
```

- [ ] **Step 2: Visually verify the SVG**

Run: `xdg-open resources/icon.svg` (or open the file in any browser).
Expected: a tilted Ace of Diamonds card on a felt-green vignette, with corner indexes and a centered diamond pip. No fallback boxes for the suit glyph.

- [ ] **Step 3: Commit**

```bash
git add resources/icon.svg
git commit -m "feat(icon): master Ace-of-Diamonds SVG source"
```

---

## Task 2: Foreground / background / splash SVGs

**Files:**
- Create: `resources/icon-foreground.svg`
- Create: `resources/icon-background.svg`
- Create: `resources/splash.svg`

- [ ] **Step 1: Create the Android foreground (card only, 78% scale, transparent bg)**

Write `resources/icon-foreground.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="1024" height="1024">
  <defs>
    <filter id="cardShadow" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="2" dy="5" stdDeviation="3" flood-opacity="0.45"/>
    </filter>
  </defs>
  <g transform="translate(100 100) scale(0.78) translate(-100 -100) rotate(-7 100 100)">
    <g filter="url(#cardShadow)">
      <rect x="55" y="32" width="90" height="135" rx="9" fill="#fffaf0"/>
    </g>
    <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
    <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
    <g transform="rotate(180 100 99.5)">
      <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
      <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
    </g>
    <text x="100" y="135" font-size="64" text-anchor="middle" fill="#c41e3a" font-family="serif">♦</text>
  </g>
</svg>
```

- [ ] **Step 2: Create the Android background (felt vignette, no card, full bleed)**

Write `resources/icon-background.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="1024" height="1024">
  <defs>
    <radialGradient id="feltGrad" cx="50%" cy="38%" r="75%">
      <stop offset="0%" stop-color="#1c4a2a"/>
      <stop offset="60%" stop-color="#0f3818"/>
      <stop offset="100%" stop-color="#072010"/>
    </radialGradient>
  </defs>
  <rect width="200" height="200" fill="url(#feltGrad)"/>
</svg>
```

- [ ] **Step 3: Create the splash (icon centered on felt at ~33% size)**

Write `resources/splash.svg`. The icon group is the same as `icon.svg`'s group, scaled by 4.51 and translated so it sits centered in a 2732 × 2732 canvas. The vignette here uses a tighter radius because the canvas is much larger than the master.

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 2732 2732" width="2732" height="2732">
  <defs>
    <radialGradient id="feltGrad" cx="50%" cy="48%" r="55%">
      <stop offset="0%" stop-color="#1c4a2a"/>
      <stop offset="60%" stop-color="#0f3818"/>
      <stop offset="100%" stop-color="#072010"/>
    </radialGradient>
    <filter id="cardShadow" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="2" dy="5" stdDeviation="3" flood-opacity="0.45"/>
    </filter>
  </defs>
  <rect width="2732" height="2732" fill="url(#feltGrad)"/>
  <g transform="translate(915 915) scale(4.51)">
    <g transform="rotate(-7 100 100)">
      <g filter="url(#cardShadow)">
        <rect x="55" y="32" width="90" height="135" rx="9" fill="#fffaf0"/>
      </g>
      <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
      <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
      <g transform="rotate(180 100 99.5)">
        <text x="64" y="58" font-size="22" font-weight="700" fill="#c41e3a" font-family="serif">A</text>
        <text x="64" y="80" font-size="22" fill="#c41e3a" font-family="serif">♦</text>
      </g>
      <text x="100" y="135" font-size="64" text-anchor="middle" fill="#c41e3a" font-family="serif">♦</text>
    </g>
  </g>
</svg>
```

- [ ] **Step 4: Visually verify all three**

Run: `for f in resources/icon-foreground.svg resources/icon-background.svg resources/splash.svg; do xdg-open "$f"; done`
Expected:
- `icon-foreground.svg`: card only on a transparent (likely white in browser) background, smaller than the canvas (78%).
- `icon-background.svg`: felt-green gradient, no card.
- `splash.svg`: icon centered on a much larger felt canvas, with empty space around it.

- [ ] **Step 5: Commit**

```bash
git add resources/icon-foreground.svg resources/icon-background.svg resources/splash.svg
git commit -m "feat(icon): foreground, background, and splash SVG sources"
```

---

## Task 3: SVG-to-PNG rasterization script

**Files:**
- Modify: `package.json` (devDeps + `assets:generate` script)
- Create: `scripts/build-icons.mjs`
- Modify: `.gitignore` (ignore `resources/*.png`)

- [ ] **Step 1: Install the rasterizer**

Run: `npm install --save-dev @resvg/resvg-js`
Expected: package added to `devDependencies` in `package.json`. No native compile (resvg-js ships prebuilt binaries).

- [ ] **Step 2: Add the build script file**

Write `scripts/build-icons.mjs`:

```javascript
#!/usr/bin/env node
// Rasterizes resources/*.svg into the PNGs that @capacitor/assets expects,
// then propagates the favicon assets into public/. The capacitor-assets
// invocation lives in a separate script and only runs when ios/ or android/
// exists; that's added in Task 5.

import { Resvg } from '@resvg/resvg-js';
import { mkdirSync, readFileSync, writeFileSync, copyFileSync, existsSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = resolve(dirname(fileURLToPath(import.meta.url)), '..');

function rasterize(svgPath, outPath, width) {
  const svg = readFileSync(svgPath);
  const png = new Resvg(svg, {
    fitTo: { mode: 'width', value: width },
    font: { loadSystemFonts: true, defaultFontFamily: 'serif' },
  })
    .render()
    .asPng();
  mkdirSync(dirname(outPath), { recursive: true });
  writeFileSync(outPath, png);
  console.log(`  rendered ${outPath} (${width}px)`);
}

const targets = [
  // Source-of-truth SVG → PNG that @capacitor/assets reads
  { svg: 'resources/icon.svg',            png: 'resources/icon.png',            width: 1024 },
  { svg: 'resources/icon-foreground.svg', png: 'resources/icon-foreground.png', width: 1024 },
  { svg: 'resources/icon-background.svg', png: 'resources/icon-background.png', width: 1024 },
  { svg: 'resources/splash.svg',          png: 'resources/splash.png',          width: 2732 },
  // Web favicon PNG fallbacks
  { svg: 'resources/icon.svg',            png: 'public/favicon-32.png',         width: 32 },
  { svg: 'resources/icon.svg',            png: 'public/favicon-16.png',         width: 16 },
];

console.log('Rasterizing SVG sources...');
for (const t of targets) {
  rasterize(resolve(root, t.svg), resolve(root, t.png), t.width);
}

// Vector favicon for the web build — copy the master SVG verbatim.
mkdirSync(resolve(root, 'public'), { recursive: true });
copyFileSync(resolve(root, 'resources/icon.svg'), resolve(root, 'public/favicon.svg'));
console.log('  copied public/favicon.svg');

console.log('Done.');
```

- [ ] **Step 3: Add the npm script**

Modify `package.json`. After the existing `"android:run"` line, add `"assets:generate"`:

```json
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "android:run": "scripts/run-android.sh",
    "assets:generate": "node scripts/build-icons.mjs"
  },
```

- [ ] **Step 4: Ignore the intermediate PNGs**

Append to `.gitignore` (under any existing project-output ignores):

```
# Icon build intermediates — committed in public/ and resources/svg only
resources/*.png
```

Note: `public/favicon-32.png` and `public/favicon-16.png` are NOT ignored — they're committed so a fresh `npm run build` works without first running `assets:generate`.

- [ ] **Step 5: Run it and verify outputs**

Run: `npm run assets:generate`
Expected stdout:
```
Rasterizing SVG sources...
  rendered .../resources/icon.png (1024px)
  rendered .../resources/icon-foreground.png (1024px)
  rendered .../resources/icon-background.png (1024px)
  rendered .../resources/splash.png (2732px)
  rendered .../public/favicon-32.png (32px)
  rendered .../public/favicon-16.png (16px)
  copied public/favicon.svg
Done.
```

Then verify dimensions and file presence:
```bash
file resources/icon.png resources/icon-foreground.png resources/icon-background.png resources/splash.png public/favicon-32.png public/favicon-16.png public/favicon.svg
```
Expected: `PNG image data, 1024 x 1024`, `2732 x 2732`, `32 x 32`, `16 x 16`, and `SVG Scalable Vector Graphics image` for the favicon SVG.

Open `resources/icon.png` and `resources/splash.png` to spot-check that the suit glyph rendered (no missing-glyph box).

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json scripts/build-icons.mjs .gitignore
git commit -m "feat(icon): SVG-to-PNG build script with @resvg/resvg-js"
```

---

## Task 4: Web favicon wiring

**Files:**
- Modify: `index.html`
- Add: `public/favicon.svg`, `public/favicon-32.png`, `public/favicon-16.png` (already produced by Task 3)

- [ ] **Step 1: Add favicon links to index.html**

Modify `index.html`. The current head looks like:

```html
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#0f3818" />
  <title>Solitaire</title>
</head>
```

Replace with:

```html
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#0f3818" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
  <title>Solitaire</title>
</head>
```

- [ ] **Step 2: Smoke test in dev server**

Run: `npm run dev` (in a separate terminal, or with `&` and then read the URL).
Expected: Vite prints a local URL (default `http://localhost:5173/`).

Open the URL in a browser and look at the tab favicon.
Expected: the Ace of Diamonds icon shows in the tab — felt green background, white card with red diamond. Not the default Vite logo.

Also visit `http://localhost:5173/favicon.svg` and `http://localhost:5173/favicon-32.png` directly to confirm they resolve.

Stop the dev server with Ctrl+C.

- [ ] **Step 3: Smoke test in production build**

Run: `npm run build && npm run preview`
Expected: build succeeds, preview server starts. The favicon should still show in the tab (now served from `dist/` rather than `public/`).

Stop preview with Ctrl+C.

- [ ] **Step 4: Commit**

```bash
git add index.html public/favicon.svg public/favicon-32.png public/favicon-16.png
git commit -m "feat(favicon): wire SVG + PNG favicons into index.html"
```

---

## Task 5: Capacitor adaptive assets + native smoke test

**Files:**
- Modify: `package.json` (add `@capacitor/assets` devDep, no script changes)
- Modify: `scripts/build-icons.mjs` (run `capacitor-assets generate` after rasterization)
- Modify: `docs/superpowers/specs/native-build-notes.md` (replace splash/icon section)

- [ ] **Step 1: Install @capacitor/assets**

Run: `npm install --save-dev @capacitor/assets`
Expected: added to `devDependencies`.

- [ ] **Step 2: Extend the build script to invoke capacitor-assets**

Modify `scripts/build-icons.mjs`. Add the import and the post-rasterization step at the end of the file (replace the trailing `console.log('Done.');`):

```javascript
import { spawnSync } from 'node:child_process';

// ...existing rasterization code unchanged...

// If a Capacitor native shell exists, regenerate its icon/splash assets.
const hasIos = existsSync(resolve(root, 'ios'));
const hasAndroid = existsSync(resolve(root, 'android'));

if (hasIos || hasAndroid) {
  console.log(`Generating Capacitor assets (ios=${hasIos}, android=${hasAndroid})...`);
  const result = spawnSync(
    'npx',
    [
      'capacitor-assets',
      'generate',
      '--iconBackgroundColor', '#0f3818',
      '--iconBackgroundColorDark', '#0f3818',
      '--splashBackgroundColor', '#0f3818',
      '--splashBackgroundColorDark', '#0f3818',
    ],
    { stdio: 'inherit', cwd: root },
  );
  if (result.status !== 0) {
    console.error('capacitor-assets generate failed');
    process.exit(result.status ?? 1);
  }
} else {
  console.log('No ios/ or android/ found; skipping capacitor-assets.');
}

console.log('Done.');
```

- [ ] **Step 3: Run the full pipeline**

Run: `npm run assets:generate`
Expected: rasterization output as before, followed by `capacitor-assets generate` output listing icons and splash images written into `android/app/src/main/res/...` and (if scaffolded) `ios/App/App/Assets.xcassets/...`.

- [ ] **Step 4: Verify Android assets were written**

Run:
```bash
ls android/app/src/main/res/mipmap-xxxhdpi/
ls android/app/src/main/res/drawable/
```
Expected: `mipmap-xxxhdpi/` contains `ic_launcher.webp`, `ic_launcher_round.webp`, `ic_launcher_foreground.webp`. `drawable/` contains `splash.png` (or similar).

Also check the adaptive icon XML:
```bash
cat android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml 2>/dev/null
```
Expected: an `<adaptive-icon>` element with `<background>` and `<foreground>` references.

- [ ] **Step 5: Build and deploy to the connected device**

Run: `npm run android:run -- --target 416db6ca` (replace device ID if different — see `adb devices`).
Expected: build succeeds, APK installs on the device, app launches.

On the device:
- The app launcher icon shows the felt-green-with-card design (not the Capacitor default cap-and-rays).
- Long-press the icon: it should look correct under whatever the launcher's mask is (circle on Pixel-derived launchers, squircle on Samsung).
- Launch the app: the splash screen briefly shows the icon centered on felt before the WebView loads.
- The browser-tab favicon (in any embedded WebView dev tools, or by visiting Capacitor's bundle URL) shows the new icon.

- [ ] **Step 6: Update the native build notes**

Modify `docs/superpowers/specs/native-build-notes.md`. The current "Splash + icon" section recommends installing `@capacitor/assets` ad-hoc. Replace that section (lines beginning with `## Splash + icon` through the end of that section's last code block) with the literal text below (note the outer fence in this plan is 4 backticks; the replacement content uses normal 3-backtick code fences):

````markdown
## Splash + icon

Source SVGs live in `resources/`:

- `icon.svg` — full-bleed master (used for iOS and the web favicon)
- `icon-foreground.svg` — card only, scaled to Android's 78% safe zone
- `icon-background.svg` — felt vignette only (Android adaptive background)
- `splash.svg` — icon centered on a 2732² felt canvas

The pipeline is wired up; just run:

```bash
npm run assets:generate
```

This rasterizes the SVGs to PNG via `@resvg/resvg-js`, copies the favicon
into `public/`, and runs `capacitor-assets generate` to populate `ios/` and
`android/`. The PNG intermediates in `resources/` are gitignored — only the
SVG sources are checked in.

Re-run `npm run assets:generate` after re-scaffolding `ios/` or `android/`
(both are gitignored by default). The script no-ops the Capacitor step if
neither native folder exists, so you can use it on web-only checkouts too.
````

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json scripts/build-icons.mjs docs/superpowers/specs/native-build-notes.md
git commit -m "feat(icon): wire @capacitor/assets into build-icons pipeline"
```

---

## Done

After all five tasks:
- `npm run dev` shows the Ace of Diamonds in the browser tab.
- `npm run build` produces a `dist/` with the favicon assets.
- `npm run android:run` installs an APK whose launcher icon is the new design under any Android OEM mask.
- `npm run assets:generate` is the single entry point for re-generating every variant from the SVG sources.

If iOS scaffolding gets added later (`npm i @capacitor/ios && npx cap add ios`), one re-run of `npm run assets:generate` populates `ios/App/App/Assets.xcassets/AppIcon.appiconset/` with the same master.
